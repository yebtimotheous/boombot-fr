# BoomBot Website - .env.example
# Copy to .env.local and adjust values as needed.

# When the backend is not running, disable API calls to avoid Vite proxy ECONNREFUSED spam.
VITE_DISABLE_API=true

# Optional: Explicit base URL for API requests in dev or production.
# If set, calls like "/performance-metrics" will be sent to this base instead of relying on Vite proxy.
# VITE_API_BASE=http://localhost:3001